<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".BookViewActivity">
<ImageView
    android:layout_width="200dp"
    android:layout_height="300dp"
    android:scaleType="centerCrop"
    android:id="@+id/coverImagePreview"
    android:src="@drawable/badgir"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    android:layout_marginTop="@dimen/image_margin"/>
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/badgir"
        android:id="@+id/titleTextPreview"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="@dimen/image_margin"
        app:layout_constraintTop_toBottomOf="@id/coverImagePreview"
        android:textSize="18sp"
        android:textStyle="bold"/>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/descriptionText"
        android:text="description"
        android:padding="@dimen/image_margin"
        app:layout_constraintTop_toBottomOf="@id/titleTextPreview"
        android:gravity="center"
        android:maxLines="20"
        android:scrollbars="vertical"
        android:layout_marginTop="@dimen/image_margin"/>
</androidx.constraintlayout.widget.ConstraintLayout>